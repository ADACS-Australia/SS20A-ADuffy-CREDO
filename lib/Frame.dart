class Frame {
  var byteArray;
  int width;
  int height;
  int imageFormat;
  double exposureTime;
  var timestamp;
}

/// frame just reformats CameraImage (from CameraImage package) is this class still strictly necessary ?
